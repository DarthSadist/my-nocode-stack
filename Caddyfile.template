# Глобальные настройки Caddy
{
    # Email для регистрации в Let's Encrypt
    email nedox32@gmail.com
    
    # Официальный ACME-сервер
    acme_ca https://acme-v02.api.letsencrypt.org/directory
}

# n8n
n8n.flowdarth.ru {
    reverse_proxy n8n:5678
}

# Flowise
flowise.flowdarth.ru {
    reverse_proxy flowise:3001
}

# Adminer
adminer.flowdarth.ru {
    reverse_proxy adminer:8080
}

# Qdrant
qdrant.flowdarth.ru {
    reverse_proxy qdrant:6333
}

# Crawl4AI
crawl4ai.flowdarth.ru {
    reverse_proxy crawl4ai:8000
}

# Netdata
netdata.flowdarth.ru {
    reverse_proxy netdata:19999
}

# WordPress
wordpress.flowdarth.ru {
    reverse_proxy wordpress:80
    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-XSS-Protection "1; mode=block"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "SAMEORIGIN"
        Referrer-Policy "strict-origin-when-cross-origin"
    }
}

# Waha
waha.${DOMAIN_NAME} {
    # Базовая аутентификация для защиты доступа
    basicauth * {
        ${WAHA_API_USERNAME} ${WAHA_API_PASSWORD_HASH}
    }
    reverse_proxy waha:3000
}
